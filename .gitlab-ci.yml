stages:
  - validate
  - plan
  - build
  - deploy

variables:
  TF_STATE_NAME: default

list_and_validate:
  stage: validate
  variables:
    TERRAFORM_IMAGE: "registry.gitlab.com/gitlab-org/terraform-images/releases/terraform:1.5.7"

    TFLINT_VERSION: "v0.61.0"
  image: $TERRAFORM_IMAGE
  rules:
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
  
      
  script:
    - gitlab-terraform init
    - gitlab-terraform fmt -check
    - gitlab-terraform validate

    - apk add --no-cache bash curl

    - curl -s https://raw.githubusercontent.com/terraform-linters/tflint/master/install_linux.sh | bash
    - tflint --init
    - tflint --recursive

terraform_plan:
  stage: plan
  variables:
    TERRAFORM_IMAGE: "registry.gitlab.com/gitlab-org/terraform-images/releases/terraform:1.5.7"
  image: $TERRAFORM_IMAGE
  services:
    - name: docker:24.0.7-dind
      alias: docker
  rules:
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'

  script:

    - gitlab-terraform init

    - gitlab-terraform plan
  artifacts:
    name: plan
    paths:
      - plan.cache

build_api:
  stage: build
  image: node:18-alpine
  rules:
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
  script:
    - cd apps/api 
    - npm install
    - npm test
  artifacts:
    name : api_build
    paths:
      - apps/api

build_client:
  stage: build
  image: node:18-alpine
  rules: 
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
  script:
    - cd apps/client 
    - npm install
    - npm run build 
  artifacts: 
    name : client_build
    paths:
      - apps/client/build

deploy_to_ec2:
  stage: deploy
  image: alpine:3.23.3
  rules:
    - if: '$CI_PIPELINE_SOURCE == "merge_request_event"'
  dependencies:
    - build_api
    - build_client
  before_script:
    - apk add --no-cache openssh-client rsync
    - eval $(ssh-agent -s)
    - echo "$SSH_KEY" | tr -d '\r' | ssh-add -
    - mkdir -p ~/.ssh
    - chmod 700 ~/.ssh
    - echo "$SSH_KNOWN_HOSTS" >> ~/.ssh/known_hosts
    - chmod 644 ~/.ssh/known_hosts
    
  script:
    - rsync -avz --delete apps/api ubuntu@$EC2_IP_ADDRESS:/home/ubuntu/
    - rsync -avz --delete apps/client/build ubuntu@$EC2_IP_ADDRESS:/home/ubuntu/apps/client/
    - ssh ubuntu@$EC2_IP_ADDRESS "cd /home/ubuntu/apps/api && npm install --production && pm2 restart my-api || pm2 start server.js --name my-api" 